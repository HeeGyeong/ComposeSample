plugins {
    alias(libs.plugins.android.library)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.kotlin.compose)

    id 'kotlin-parcelize'
}

apply {
    from('../config.gradle')
    from('../core-dependencies.gradle')
}

android {
    namespace 'com.example.coordinator'

    buildFeatures {
        compose true
    }
}

dependencies {
    implementation(project(':core'))

    // coordinator 모듈은 CoordinatorActivity + Composable UI를 포함하므로
    // Compose 의존성을 직접 선언합니다.
    // (material3 는 core-dependencies.gradle 에서 공통 제공되므로 여기서 중복 선언하지 않습니다 — DEP-DUP-01)
    def composeBom = platform(libs.composeBom)
    implementation(composeBom)

    implementation(libs.activityCompose)
    implementation(libs.composeUi)
    implementation(libs.runtime)
    implementation(libs.uiToolingPreview)

    implementation(libs.bundles.lifecycle)
    implementation(libs.bundles.koin)
}